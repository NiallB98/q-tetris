export Q_CMD=""            # Manually set the Q command used to start Q scripts in run.sh and start_server.sh if the default settings aren't working
export FPS=""              # Manually set frames per second to run the game at (By default 60 fps)